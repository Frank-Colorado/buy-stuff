import Directory from '../components/directory/Directory';

const HomePage = () => {
  return (
    <section style={{ height: '100%' }}>
      <Directory />
    </section>
  );
};

export default HomePage;
